spring:
  application:
    name: rpc-api
  datasource:
    url: jdbc:postgresql://localhost:5432/RPCDB
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect